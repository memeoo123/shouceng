# Cocos restoration progress

## Confirmed

- Cocos Creator 3.8.8 project skeleton based on the installed official `empty-2d` template.
- Stage 2 design resolution is 750×1286 with fixed-width scaling.
- Forest background, tree, castle, opening-shop buildings, HUD/button art and HP bars are copied byte-for-byte from the recovered Laya asset output.
- Blue/red Swordsman1 and Shooter1 idle/move/attack/victory frame sets are present; frame interval is 102 ms.
- Stage 2 map, 3/4/7/9/12 wave counts, 975 castle HP, opening shop, obstacle prices and spawn delay bounds are encoded.
- Pure TypeScript tests cover dodge-before-critical, normal/critical damage, base HP, coin allocation, placement cells, deterministic Stage 2 rosters, nearest-target selection, movement, route angles, swept contacts and terminal conditions.
- Creator imported all current source assets, the project checker reports 1,500 asset files with 0 missing metadata, and the Creator web-mobile build compiles the project TypeScript successfully.
- The web-mobile build loads at 574×1036 with no browser console errors; zero-cost tree clearing, opening-shop drag/drop and combat presentation switching were exercised interactively.
- 475 critical source PNG/frame hashes were compared against the Laya reference output with 0 mismatches.
- The Cocos runtime now schedules all five Stage 2 roster sizes, moves enemies toward the nearest live target with the confirmed 600-pixel acquisition bound, and falls back toward the castle.
- Swordsman and Shooter attacks use their recovered 102 ms action sequences and fire frames; ranged attacks use visible projectiles while melee resolves at the action fire frame.
- Attack state now carries a deterministic lock timer advanced by the same combat delta as movement and cooldowns, while the visual callback remains an optional early completion signal. A normal-value diagnostic reproduced the old latch as `attacking=true` with an already negative cooldown; the recovered `frameCount × 0.102s` timer removes that permanent combat stall for both teams without changing attack interval, fire frame or damage.
- The opening barracks produces Shooter1 allies, the arrow tower acquires enemies and fires, buildings and units display damage-state HP bars, and the castle HP reaches a tested defeat transition.
- The defeat overlay and in-scene retry reload were exercised in the 574×1036 browser viewport with zero console errors.
- Deployed buildings can be picked up during non-combat preparation, moved to another valid footprint, returned to their vacated shop slot, and deployed again; the same handler remains active when the shop returns between waves.
- Normal wave completion now waits for the roster, living enemies and all in-flight projectiles to drain; surviving allied units are retained and returned to idle, combat actors are hidden during preparation, the ordinary shop rolls again, and the next wave starts only after the player completes preparation.
- The normal-value `test=cocos-normal-lifecycle` run now completes waves 1–5 without the high-damage/high-durability balance adapter. Its input-only automation clears trees through the recovered 0/1/2/3/4/5 coin path, deploys ordinary inter-wave offers through the production placement handler, falls back to production synthesis when no footprint remains, and selects one visible combat-priority trait. Runtime ended with wave 5 at 12 spawned/12 resolved, castle HP 975, fight level 7, victory, no enemies/projectiles and zero captured errors.
- Combat fire-frame delays now share the deterministic combat clock used by movement, cooldowns and the attack-action lock. The normal-lifecycle test alone executes 30 fixed substeps per rendered frame so browser background throttling cannot turn a valid five-wave run into an apparent stall; normal gameplay still executes one step at the original speed.
- Recovered 1.5–2.4 second route heading lifetime, 0.2–0.6 radian jitter, distance attenuation, 50-pixel same-team collision radius, 1.1 contact weighting and 50–700 map bounds are implemented.
- Friendly and hostile ranged projectiles now use the recovered expanded-box segment test and resolve the first swept unit/building/castle contact.
- Stage 2 now applies recovered elite probabilities `[0, 0.3, 1.2, 2, 0.5]`; final wave index `floor(12/3)` is deterministically replaced from the `stageId*1000` boss stream, producing the Shooter boss for Stage 2.
- Stage 2 elite/boss stats, 1.5×/2× visual and route-collider scale, boss stay-equal-collider exemption and boss 100-pixel full-damage AOE are implemented from the recovered unit table.
- Ordinary building, allied-unit and hostile-unit arrows now retain their last velocity after the original target dies, remain eligible for path interception and expire by the recovered unit/building lifetime formulas; building auto-flow uses the recovered 30ms check and one-sided 10-degree turn cap.
- The explicit `test=cocos-lifecycle` balance adapter completed waves 1–5, consumed the final 12/12 roster and reached victory with no captured runtime errors; normal entry was separately checked with the adapter absent.
- The lifecycle adapter now separates its 2000-pixel full-field acquisition radius from each tower's recovered projectile lifetime/range input. Runtime diagnostics reduced Arrow Tower tail lifetimes from the accidental two seconds to roughly 0.89–0.94 seconds; its adapter-only fire cooldown is 0.1 seconds to avoid producing dozens of redundant tail arrows while preserving the same production combat path.
- Recovered 58px combat damage labels now use a 5px dark outline, exact normal/critical/dodge/building-hit colors, rise at 42 pixels/second and fade over 0.85 seconds; the five-wave smoke created 41 labels with a peak of 7 active and no runtime errors.
- Enemy elite/boss outgoing repel uses 12 physics units/second, elite 0.3-second and boss 0.35-second duration, remaining-time linear attenuation and map clamping; boss `RepelResist` rejects incoming repel. The controlled production-path `cocos-repel-before`/`cocos-repel-after` pair records the elite target at y `500→560→600→620` over 0.3 seconds (120px total), while the Stage 2 Shooter Boss remains exactly at `[500,350]` after a Knight charge repel attempt; both routes pass with zero browser errors.
- The projectile core supports recovered `deadInLast` death-position latching, auto-flow disable, 50×50 fake contact, within-50 immediate consumption and `forceTargetOnly`; normal arrows keep the default disabled state.
- Recovered air-support buttons are restored in the original meteorite/healing/freeze order, dock to the actual fixed-width viewport bottom, and become available only during combat; each support is single-use for the whole battle.
- Meteorite snapshots all live enemies and launches one 9999-damage, 65-physics-unit/second `deadInLast`/`forceTargetOnly` projectile per target over the recovered three-second window; healing restores all live allies by 100% max HP and freeze halts all snapshot enemies for four seconds.
- The explicit `test=cocos-air-support` adapter passed at a 400×720 portrait viewport: the support strip ended exactly at visible design-space y=1350, all three meteorites impacted, one ally healed 56.25 HP, three enemies froze, repeat use was rejected and browser errors remained empty.
- Air-support presentation now uses the exact locally recovered FairyGUI JTA resources rather than placeholder rings: 7-frame `Pic-XunHuanHuo` projectile at 1.8×, 10-frame `ArtilleryFire` hit at pivot `[0.5,0.8]`/1.7×, 18-frame healing at pivot `[0.5,0.63]`/2× and the persistent one-frame freeze shell. All sequences preserve their 83ms interval, source canvas offsets and blank healing frame 0.
- The original 2,171,644-byte `OPPOSansH.ttf` is imported byte-for-byte (SHA-256 `ad6bdbd3…b50a9c`) and assigned through both label factories. Runtime evidence shows 19 preparation labels and 23 labels after dynamic combat text using the recovered font with zero console errors.
- Stage 2 runtime economy now assigns exactly 10 coins across each wave's enemies (`[4,3,3]` through `[1×10,0,0]`) instead of awarding one coin per kill. The five-wave lifecycle audit ended at exactly 50 coins, and the preparation HUD uses the recovered counter art while all remaining tree badges advance from 0 to 1 after the first free clear.
- Same-ID/same-level field synthesis is restored for the three opening building families through level 4, with exact per-level sprites and arrow-tower weapon mounts. Runtime synthesis merged two level-1 `e07` towers into one level-2 tower with attack 30, `Building_ArrowTower2` plus `_up`, one remaining building and zero browser errors; upgraded pieces use their current definition on later drags.
- Shop-area synthesis now follows the recovered overlap rule for the same opening families: an available same-ID/same-level target upgrades while the source slot is consumed. The portrait runtime smoke produced a level-2 `e07` with attack 30 and matching body/weapon sprites, left the target available, hid the source slot and captured zero errors.
- The first expanded catalog family, `e01`/`Building_Mauler`, is restored through levels 1–4 with four byte-identical sprites, 520 HP, recovered 10/8/7/6-second production cooldowns and 2/4/6/8 summon caps. Its production path creates the already recovered `Swordsman1`; a runtime smoke attempted five immediate productions and stopped at exactly two live level-1 allies with zero errors. The existing opening level-2 `e02` cap was corrected from the temporary value 2 to the recovered value 4.
- `e06`/`Building_Swordsman` is restored through levels 1–4 with its 3×1 footprint, 390 HP, 9/7.2/6.3/5.4-second cooldowns and 2/4/6/8 summon caps. Its `Mauler1` unit now uses 400 HP, attack 10, 2/3 attack speed, 0.9 movement speed, one-cell melee range, zero bullet speed and attack fire frame 4. All 38 blue-team idle/move/attack/victory frames load at recovered counts 8/10/7/13; runtime production stopped at exactly two live level-1 units with zero errors. The older temporary Swordsman1 bullet speed 5 was corrected to the recovered melee value 0.
- `e04`/`Building_Crossbowman` is restored through levels 1–4 with its recovered T footprint, 520 HP, 11/8.8/7.7/6.6-second cooldowns and 2/4/6/8 summon caps. Its `Crossbowman1` unit uses 75 HP, attack 35, 0.8 attack speed, speed 1, six-cell range, bullet speed 15 and attack fire frame 6. All 40 blue-team frames load at counts 8/10/9/13; runtime production capped at two, created its projectile only after fire frame 6, and applied exactly 35 ranged damage with zero browser errors.
- A deterministic production-path Crossbowman phase capture now freezes recovered attack frames 5/6/8 at 0.510/0.612/0.816 seconds. Frame 5 has no projectile, frame 6 creates the arrow, and frame 8 records the completed 35-damage hit (`300→265` HP). The capture also fixed a floating-point residual that could postpone an exactly aligned fire-frame callback by one 102ms frame; the scheduler now uses a one-microsecond completion tolerance.
- `e03`/`Building_Knight` is restored through levels 1–4 with its recovered hollow 3×2 footprint, 650 HP, 13/10.4/9.1/7.8-second cooldowns and 2/4/6/8 summon caps. `Knight1` uses 350 HP, attack 40, speed 1.7, melee range and fire frame 9; all 50 frames load at counts 8/10/14/13 plus 5 charge frames. The optional trait-21/23 path is runtime-confirmed: 1.5× charge speed, 0.3× first-hit damage, 10-unit/second 0.2-second repel, three-second dizziness, charge consumption and base-speed restoration. The trait is not enabled for plain knights unless selected.
- `e05`/`Building_Mage` is restored through levels 1–4 with its recovered L footprint, 520 HP, 11/8.8/7.7/6.6-second cooldowns and 2/4/6/8 summon caps. `Mage1` uses 100 HP, attack 60, 7/12 attack speed, speed 1.1, 4.5-cell range, bullet speed 19.5, fire frame 9 and its innate 100-pixel AOE. All 46 frames load at counts 8/10/15/13; a real projectile hit damaged the primary and a 70-pixel-near enemy for 60 each while leaving a 140-pixel-outside enemy untouched.
- `e08`/`Building_Trebuchet` is restored through levels 1–4 with eight byte-identical body/weapon sprites, its recovered 2×2 footprint, 520 HP, attacks 60/90/138/210, four-second cooldown, 650-pixel range and 5%/1.5× critical rule. Its throw projectile uses the recovered 1250-pixel/second speed and 48×26 size, contacts only the locked target, and applies full damage plus 0.5× secondary damage within 75 pixels. Browser smoke confirmed 60 primary, 30 nearby and 0 outside damage, the original weapon mount, and zero runtime errors.
- `e09`/`Building_ElectricityTower` is restored through levels 1–4 with eight byte-identical body/weapon sprites, its recovered 2×1 footprint, 260 HP, attacks 40/60/92/140, three-second cooldown, 700-pixel range and 5%/1.5× critical rule. Its light projectile uses the recovered 6000-pixel/second speed, 38×20 size and fixed 0.3-second lifetime; after its locked-target hit it retargets once to a random unhit enemy within 700 pixels. Browser smoke confirmed 40 primary, 40 first-jump and 0 outside damage, the original weapon mount, and zero runtime errors.
- `e10`/`Building_MirrorTower` is restored through levels 1–4 with eight byte-identical body/weapon sprites, its recovered vertical 1×2 footprint, 260 HP, attacks 20/30/46/70, four-second cooldown, 750-pixel range and 5%/1.5× critical rule. Its laser applies one immediate hit plus seven 0.25-second ticks during the two-second lifetime. All four exact 15-frame FairyGUI `Laser1`–`Laser4` MovieClips are extracted locally at the recovered 83ms interval with their 556×232 canvas offsets preserved; runtime capture visibly connected `Laser1` from the exact weapon mount to the target and confirmed 20 immediate/160 total damage, eight hits, timed removal and zero browser errors.
- `e11`/`Building_ShuiJing` is restored through levels 1–4 with four byte-identical 78×87 sprites, its recovered 1×1 footprint, 130 HP and 5%/10%/20%/40% active experience bonuses. Result experience rounds the unfixed enemy value after the building multiplier, while fight-level experience first floors the recovered 0.833333333333333 global correction and then applies the multiplier. Runtime smoke confirmed a normal enemy yields 11 result experience and 8.4 fight-level experience with a level-1 crystal, returns to 10 and 8 while the crystal is inactive, and captures zero browser errors.
- `e12`/`Building_Well` is restored through levels 1–4 with four byte-identical 72×167 sprites, its recovered vertical 1×2 footprint, 260 HP and 5%/7.5%/11.5%/17.5% enemy movement slow values. Each active well divides enemy speed by `1 + amount`, multiple wells stack multiplicatively, and disabled/destroyed wells stop contributing. Runtime movement measured 13.095238 pixels with a level-1 well versus 13.75 without it (ratio 0.952381), matching `1 / 1.05`, with zero browser errors.
- `e13`/`Building_ObservationDeck` is restored through levels 1–4 with four byte-identical sprites, its recovered vertical 1×2 footprint, 260 HP and 5%/7.5%/11.5%/17.5% global critical-chance bonuses. The active-only bonus is additive, applies without adjacency to both defense weapons and barracks units, and is capped with final critical chance at 95%. With a controlled 0.08 roll, browser smoke confirmed the level-1 deck raises a 5% base chance to 10%: Mirror Tower damage changed from 20 to 30 and allied Swordsman damage from 30 to 60; both returned to normal while the deck was inactive, with zero errors.
- `e14`/`Building_Statue` is restored through levels 1–4 with four byte-identical 101×196 sprites, its recovered vertical 1×2 footprint, 260 HP and 5%/7.5%/11.5%/17.5% adjacent attack-speed bonuses. Orthogonal and diagonal occupied-cell contact both count; active bonuses add into `1 + sum` and divide both defense attack cooldown and barracks summon cooldown. Runtime smoke confirmed a level-1 statue changes adjacent Arrow Tower/Swordsman barracks cooldowns from 2/9 seconds to 1.904762/8.571429 seconds, and both return to 2/9 when the statue is inactive, with zero errors.
- `e15`/`Building_MartialArtsField` is restored through levels 1–4 with four byte-identical sprites, its recovered horizontal 2×1 footprint and 260 HP. Active fields add 5% attack to adjacent defense towers and units produced by adjacent barracks; occupied-cell orthogonal or diagonal contact counts, multiple fields add into `1 + sum`, and inactive/destroyed fields stop contributing. The shipped table's `Ex.AtkAdd` synthesis key does not modify the gameplay `Ex.RoundAtkAdd` field, so all four levels deliberately retain 5% while changing sprites.
- `e17`/`Building_Fence2` is restored through levels 1–4 with four byte-identical sprites, its recovered horizontal 3×1 footprint and exact 507/760.5/1166.1/1774.5 HP values. It participates in the ordinary enemy nearest-building target set as `WallMaterial`, takes the normal damage path and remains active until its HP reaches zero.
- `e18`/`Building_Mine` is restored through levels 1–4 with four byte-identical sprites, its recovered 1×1 footprint, 130 HP and exact 1/2/4/8 coin payouts. Every active surviving mine pays after a non-final wave; destroyed mines and the final wave pay nothing. Runtime progression from 10 coins measured 11 after level 1, stayed 11 after destroying it, reached 19 after an active level-4 payout, and stayed 19 after the final wave.
- Stage 2 paid/special shop refresh is active across the complete 18-building functional catalog. It uses recovered ordinary/gold/slot weights `887/100/13`, wave-scaled level-2/3 weights, one free manual refresh followed by 15-coin charges, a three-regular-refresh slot-expansion guarantee, insufficient-funds rejection and a one-use special refresh whose first building is level 2. Runtime smoke measured coins `30→30→15→0→0`, expanded one locked cell, kept the special roll outside the regular counter and captured zero errors; only the original network/ad request is intentionally replaced by a local one-use gate.
- Fight-level trait selection is restored from the shipped 30-level experience table and 16-row general-trait table. Crossing the first 20-point threshold raised level `0→1`, discarded overflow, retained the live battle in a paused state and displayed three unique choices with at most one legendary option. A 5% all-unit attack choice changed Shooter attack `23→24.15`; clicking the visible immediate-coin choice closed the panel, resumed selection state and changed coins `0→20`, with zero browser errors. The panel uses the supplied portrait reference silhouette and hides combat HUD while open; the ad/network-backed refresh and get-all controls remain deliberately deferred.
- The original `fightLvPrgs` HUD is restored as a fight-level experience bar rather than a wave-kill bar: exact FairyGUI geometry is 610×22 at `[39,126]`, with a 604×16 fill and the byte-identical 70×42 level badge at `[641,115]`. A normal-value Stage 2 browser run (no combat balance adapter) deployed the three opening pieces, showed `16/20` after two kills, reached level 1 after the third, accepted the visible `+20` coin trait, returned to wave-2 preparation with 30 coins and 975 castle HP, and captured zero browser errors.
- The original MP4 now has reproducible five-frame motion windows at `11.796–12.204s` and `17.796–18.204s`, sampled every 102ms through the installed Chrome decoder and preserved under `evidence/original-motion-windows`. Alpha-core tracking confirms real position changes and candidate animation-phase changes in both windows: t12 has 2/3 actors with at least four reliable center samples, while t18 has 4/5. Tree/actor occlusion and low runner-up margins are retained as explicit low-confidence evidence instead of being promoted to exact phase claims.
- Dedicated `cocos-visual-motion-t12`/`cocos-visual-motion-t18` routes now render every `-204/-102/0/+102/+204ms` evidence sample by URL, publish the selected offset and confidence flags, and remain presentation-only. All 10 browser captures completed with zero game/page/non-favicon HTTP errors. Across 34 pairs whose original and Cocos center detections both pass the conservative gate, the maximum per-axis center delta is 0px; the ten-frame averages are RGB MAE `0.067078` and edge IoU `0.181037`.
- Victory and defeat settlement now follow the recovered Laya baseline: a full 750×1286 dimmer, 540×390 green/red panel at `[105,400]`, matching title/subtitle/statistics, cumulative kills, result experience and local retry. Victory before the final stage adds the recovered green `下一关` action and an unlock label when the frontier advances. Browser validation drove Stage 1 victory from `[1,0]` to `[2,0]`, reloaded ordinary Stage 2 successfully, and confirmed an ordinary Stage 220 request is bounded back to unlocked Stage 2. The Stage 2 wave-2 defeat adapter produced `[2,1]` without advancing.
- The recovered offline stage foundation is now inside the Cocos resources tree byte-for-byte: all 220 stages, 2,620 waves, 60,588 roster entries, 660 chest bundles, six base enemy records, 18 variants and all three map definitions. Exact 750×1623 Desert/Snowfield backgrounds are imported alongside Forest. Production battle initialization now reads the requested stage's map, 7×9 grid, wave counts, wave power, elite probability, final-boss conversion, enemy pool, shop weights and name; castle placement uses the recovered bottom-up centered search. All 1,016 recovered red-team action PNGs for six families across levels 1–4 are imported byte-for-byte. The `cocos-campaign-stage-battle` route started Stage 220 through the production path, preloaded all 18 visual bodies required by that stage, built its exact two-enemy first-wave roster and reached normal defeat settlement without missing-frame errors.
- The Cocos campaign page now provides offline selection across every unlocked stage, exact stage names/maps and original `declaredWave` records, then enters the selected production battle through `battle=1`. The version-scoped `LocalProfile` migrates the legacy `MaxStageRecord` and persists typed `Prop`/`Item` counts plus idempotent `WaveChest_stage_index` flags without inventing starting assets. Stage 2 `[2,5]` browser validation claimed the 3/5-wave bundles for 600 silver and 10 stamina, rejected the locked 10-wave bundle without mutation, and retained explicit manual claiming instead of victory auto-grant. The actual `[2,0]` profile switched between unlocked Stages 1/2 and entered Stage 2 normally; network remained disabled.

## Approximate

- `ShouchengGame` builds the preparation scene from exact resources and coordinates, supports tree clearing, shop drag/drop and the first combat presentation switch. The functional campaign card uses recovered maps/buttons/font and exact rules, but its Cocos-native arrangement is not claimed pixel-identical to the original FairyGUI meta page.
- Target caching, randomized route steering, scaled same-team separation and swept contacts are wired into Cocos nodes; native simultaneous-contact ordering remains approximated by deterministic first swept contact.
- The five-wave scheduler and victory predicate are browser-verified both under the explicit balance adapter and under normal combat values with an input-only preparation adapter. The automated preparation strategy is deterministic evidence, not a claim that every arbitrary player layout wins.
- Damage-text lifecycle is runtime-verified. Dedicated `cocos-visual-drag`, `cocos-visual-prep`, `cocos-visual-wave-start` and `cocos-visual-combat` routes reuse the recovered t02 drag case or exact representative layout and a route-only seed. The t12/t18 routes freeze eight actors at reference-matched position/action/frame/facing values derived from the original PNGs and recovered local unit sequences; every candidate is evaluated in the actual `idle_0` runtime display box, with both horizontal orientations and a 60% visible-alpha-core score to tolerate tree/UI/actor occlusion. t12 now uses Shooter move 8 flipped plus Swordsman move 9/6; t18 uses Swordsman move 9, Shooter move 7 flipped, Swordsman move 1 and allied Shooter idle 2/4 with the second flipped. Normal gameplay remains on the production simulation and animation path. The new 574×984 captures improve t12 MAE `0.066993→0.065218` and edge IoU `0.169552→0.178195`; t18 improves MAE `0.071775→0.069135` and edge IoU `0.173100→0.182650`. All `3/3` t12 and `5/5` t18 alpha-mask pairs pass the conservative fit gate and their detected centers differ by at most one pixel.
- Original-side adjacent-frame tracking is now automated by `tools/analyze_visual_motion_windows.py`. It walks independently forward/backward from each matched anchor, records frame hashes, center motion, candidate action/frame/facing, search-boundary hits and separate center/phase confidence. The observed 102ms samples prove the reference is not static, but compression, overlap and several low fit margins do not yet justify changing the production `SpriteSequence` interval.
- Cocos-side evidence replay and comparison are automated by `tools/compare_visual_motion_windows.py`. The ten routes reproduce measured state at an explicitly selected offset and prove sprite rendering/coordinate placement, not continuous production simulation or interpolation equivalence.
- Air-support rules, bottom docking and all locally available per-target FairyGUI MovieClips are runtime-verified; the separate full-screen `Effects_MeteorSwarm`/`Effects_Frozen`/`Effects_Rebleeding` resources are absent from the recovered local package set, so no unsupported overlay was invented.
- Exact glyph rendering is restored; matched t02/t07/t12/t18 wave-label white-glyph bounds are now measured in `evidence/COCOS_VISUAL_DIFF.json`.
- The electricity projectile uses the closest ordinary Cocos sprite tint `[125,230,255,255]`; the recovered Laya additive color-filter matrix cannot be represented exactly by a standard Cocos `Sprite.color` and remains a low-severity presentation approximation.

## Missing

- The ad/network-backed trait refresh/get-all controls, equipment-only trait families, non-air-support skills, the remaining four-tab offline meta shell (shop/buildings/talents/dungeons), representative visual captures outside Stage 2 and full-lifecycle sweeps for all 220 stages. `MaxStageRecord`, the campaign selection card, explicit wave-chest claims, ordinary-entry lock enforcement, result retry/main-page/next-stage navigation are connected. The ordinary three-choice general-trait path and all 16 recovered general-trait definitions are represented.
- The three full-screen `Effects_*` air-support overlays referenced by code but absent from local FairyGUI binaries/atlases; network/lazy-resource recovery remains intentionally deferred.
- A same-timestamp production-path Cocos capture is still needed before any pixel-perfect continuous-animation claim. Original and evidence-replay sides now both have two 408ms/five-frame windows, but the replay intentionally selects measured states directly; the production-path Crossbowman windup/fire/recovery sequence is separately captured and stable.
